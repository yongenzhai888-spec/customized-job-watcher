__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

.env
out/
.cache/
# 本地匹配口径与 Top10 名单：口径由业务地图提炼，刻意不进仓库、不上传 GitHub
# 注意要写成 local/* 而不是 local/ —— 整个目录被忽略时，里面的 !例外 不生效。
local/*
# 例外：名单本身只是岗位清单（标题/链接/分数），需要提交回仓库，
# 否则 Actions 每次跑都是全新环境，名单存不住、也无法判断“今天是否已推过”。
!local/top10.json
